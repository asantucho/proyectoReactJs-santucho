import React from 'react';
import CartIcon from '../images/cart-shopping-solid.svg';

const CartWidget = () => {
  return <img className='cart-icon' src={CartIcon} alt='Cart icon' />;
};

export default CartWidget;
